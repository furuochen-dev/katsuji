# 排版过程

先 `Katsuji.apply`，再 `Katsuji.applyHangAvoidance`。切缝扫根下文本（跳过 `pre` / `code` 等）；挤推、撑行只动段落、标题、列表。

Demo 的「下一步」一次做完**一行**（第 3–5 步）。段末行跳过第 5 步。

## 标点属性

默认表在 `punctuation-rules.js`。汉字、英文、数字不在表里：两边都不插缝，行头行尾都合法。

| 属性 | 字符（默认） |
|---|---|
| `前有空` | `（` `【` `「` `『` `《` `〈` `〔` `［` `“` `‘` `(` |
| `后有空` | `，` `。` `、` `；` `：` `？` `！` `）` `】` `｝` `〉` `》` `」` `』` `)` `]` `}` `”` `’` `·` `;` `:` `!` `?` |
| `两侧无空` | `…` `～` `—` `％` `‰` `℃` `°` |
| `不能在行头` | `后有空` 和 `两侧无空` 全部 |
| `可悬挂` | 见下。是 `后有空` 的子集，不是禁则 |

`不能在行头` 不是另造一张表，就是 `后有空` + `两侧无空`。

`可悬挂` 只在打开标点悬挂时有字。无参 `applyHangAvoidance` 不开悬挂，没有字是 `可悬挂`。打开后 `hangRight: 'stops'`（打开时的默认）是 `，` `。` `、`；`'all'` 是全部 `后有空`；`'none'` 仍没有。半角 `,` `.` 不进 `后有空`（还没有英文支持；数字小数点也不能调）。左边没有对称属性；左挂两档只改第 3 步要不要再推盒。

行尾禁则没有单独一张表：**不能出现在行尾的就是 `前有空`**。

`setPunctConfig` / `applyPunctPreset('jis-strict'|'vertical')` 会改这张表：JIS 严格把小假名、长音、叠字等并进 `两侧无空`；竖排把 `？` `！` 改成 `两侧无空`，并加上竖排形标点。

## 怎么跑

1、2 对整篇文章做一次。`applyHangAvoidance` 开头先清掉上一轮挤推、半角盒（含连写收的），补回连写删掉的缝，以及我们上一轮加上去的悬挂 padding。至少一侧要挂时，每个挤推块先做 **2′**，再从第一行做到倒数第二行：一行上先 3、再 4、再 5。第 5 步要看**还没处理的下一行**，所以从前往后。

| 谁 | 不做 |
|---|---|
| 段末行 | 第 5 步（不挪、不包、不挂） |

两侧都不挂：没有 2′，第 3、5 步与不开悬挂时相同。排完不恢复浏览器断行，`line-break: anywhere` 留着。

第 3、4 步**只看本行里的字**。跨行的 `」` / `「` 不当连写。

## 量宽和 selector

**行宽一律是正文框，不含悬挂 padding。** 量内容盒（`clientWidth − padding`）。开启悬挂时左右各加 0.5em，量到的盒子已经不含沟，直接当行宽。若只有一侧加了 0.5em，量到的盒子还含另一侧的假剩余，再减去那 0.5em（例如只右加：27.5 − 0.5 → 27）。沟不是行宽，也不是行内剩余。沟只吃剩余，行宽不低于本行的合。**段首行再减 `text-indent`。**

**合 = 字 + 空 − 半**。字按 DOM 逐字加；空是本行缝的 padding/margin；半是盒比字少占的内口。跨行连写不缩短本行。

| 盒 | 占内口 | 半 | 基数相对未包 |
|---|---|---|---|
| 未包 | 1em | 0 | 0 |
| 半角顶格（不可悬挂的 `后有空`） | 0.5em | 0.5 | ±0.5 |
| 半角已推出（`可悬挂` / 左挂） | 0（不占正文框） | 1 | ±1 |

第 5 步的基数是这些字**挪完之后**在正文框里的占宽，不是毛宽相加。再和行宽合成：

- 压入量 = 基数 + 合 − 行宽
- 推出量 = 基数 + 行宽 − 合

未包半角的 `后有空` 先按 1em 计。包半角：不可悬挂 −0.5 / +0.5；可悬挂不占正文框，−1 / +1。汉字、`前有空`、`两侧无空` 未顶格时按 1em。

例：合 27em，行宽 27em（2′ 不计入）。抽 `」。`：2 − 0.5 连写 − 1 悬挂 = 0.5。压入量 = 0.5 + 27 − 27 = 0.5，行内挤 0.5em。若误用 2′ 后的 27.5em 当行宽，压入量变成 0，该挤的没挤。

挪完会出现新的成对标点（第 4 步那种）。每收一处 −0.5em（前面的字进半角盒、中间缝删掉），都要从基数里减：

- **压入：** 原行尾 + 抽上来的第一个字；以及抽上来的那一串内部（下一行还没做第 4 步，串内尚未收）。抽完立刻按第 4 步收这些对：接缝和串内都收，不要只打接缝。抽上来还没折到本行时也先收，折行器才能看见少掉的 0.5em。
- **推出：** 推下去的最后一个字 + 原下一行行头。串内若本行第 4 步已经收过，已在「合」里，不要再减一遍。

不减的话，差 0.5em 去抽 `。」` 会按 1em 计，selector 可能改推；实际连写后只多占 0.5em，本该压入。推到下一行头碰上 `前有空` 同理。

然后 selector：

- 压入不超过 0.6em（允许多 0.01em）。超上限：有推出就推、没有就跳过
- 压入量 ≤ 0（剩余已够放下）：仍压入，不抽缝，没有可调缝也可以
- 压入量 > 0：要有可调缝来摊，挤还是推看哪边每条缝摊到的绝对值更小，差不多时默认挤
- 推出量不是正数、或每条缝分到不到 0.01em，当没这回事

哪几条缝能摊：

- **压入：** 本行行内缝，加上行尾 `后有空` 后面那条。抽上来之后这条变成接缝，要拿来摊压入量。本行没有别的缝、只剩这一条，也可以抽。
- **推出：** 只改本行行内缝。行最末按推完留下来的最后一字算：它后面那条、行首已顶格锁死的缝都不动。本行末尾被推走的 `前有空` 前面那条，推完就是下一行开括号缝，也不摊。连写不再占一条不可调缝，前面的字已经是半角盒。

---

## 1. 标记可调整空间（整篇）

在标点外侧插一条宽度为 0 的缝，后面全靠改这条缝的空白来挤、推。`后有空` 插在后面，`前有空` 插在前面，`两侧无空` 两边都不插。同一段字只该跑一遍，再跑会再插一层缝。

`Katsuji.apply` · `segmenter.js`

## 2. 暂时禁用浏览器自动断行（整容器）

浏览器自己会把 `不能在行头` 拽回上一行，后面的修正就看不到行首禁则。先改成任意字都可以折行，排完也不改回来。

`relaxBuiltinLineBreak` · `line-break.js`

## 2′. 留悬挂沟（每块一次；至少一侧要挂才做）

给这个块**在现有 padding 上左右各再加 0.5em**。沟叠在作者边距外面，**不计入行宽**。关掉悬挂或重跑前，只减掉我们加的那两侧。重跑只加一次。

开启悬挂时左右各加 0.5em，为了对称。不改作者的 `text-indent`。左挂两档只管第 3 步要不要再推盒，不控制这条沟。

例如：原来 0 → 0.5em；原来 0.2em → 0.7em；原来 1em → 1.5em。

无参 `applyHangAvoidance` 不开悬挂，不做这一步。

## 3. 行首开括号顶格（每行）

行首是 `前有空`：去掉它左边的前空。左边那条缝清零并标成不再动；包一个 0.5em 宽的窗口，里面的字左移半个字，只露出右边的墨。段首行也做：缩进还在，只是开括号不再带着前空。不是段首行时和后面**第一个字**绑进同一个 `inline-block`，半宽后仍超过上一行剩余，不会被抽回去。只绑这一个字，后面连续标点不进这个盒。

左挂两档，都不改作者的 `text-indent`：

- **`hangLeftIndent`：** 打开悬挂时的默认。只在**段首行**且 `text-indent ≥ 0.5em` 时再推盒，墨进缩进。没有缩进、或不够 0.5em，不做（不会借左沟凑）。
- **`hangLeft`：** 默认关。每一行（含段首）再推盒，墨进 2′ 左沟。段首若同时也开了缩进左挂，先吃缩进，不够的进沟。

要推时：**先绑**，再把这个 0.5em 盒向左推 0.5em，占内口 0。推完盒本身是 0 宽；`line-break: anywhere` 下上一行剩余 0 也会把裸着的 0 宽盒单独抽到行尾、和末字重合。必须已经和后一字在同一个 `inline-block` 里（不能只靠 inline 的 `white-space: nowrap`）。合、基数仍按占内口 0 计，不看这个外包盒。后一字若是标点，第 4 步可能把它也收成半角，原子盒变成约 0.5em；上一行剩余够时**整对**抽上去，不是 0 宽盒独自回去。这不是问题。

不改作者的 `text-indent`。推盒是负 margin，缩进还在。

```
（文本文本文本...
```

`trySpaceOnEdgeStart`

## 4. 标点连写间距收缩（每行，只看行内）

成对条件：`后有空` 后面紧跟任意标点，或任意标点后面紧跟 `前有空`。连续 `两侧无空`（如 `……`）绑在一起，中间不折开。

做法：把**前面那个字**收成半角盒，中间那条缝删掉。合少 0.5em，和以前 −0.5em 锁缝一样；折行器看见的也是 0.5em，后面抽上来的成对才能跟着重排。前面已经是半角盒（比如行首顶格）时，改收后面那个字。`后有空` 盒露左墨；`前有空` 和行首顶格一样，盒里左移半字只露右墨，否则会露出空白左半、墨叠到下一个字上。可调空只数还在的缝，不用再排除连写缝。

**4.1** `后有空` 紧跟 `前有空`（如 `）（`）：中间有两条缝，只删**前一条**（后有空后面那条），后一条不动；`）` 进半角盒。

```
文本）（文本文本...
```

**4.2** 其余成对：两字中间只有一条缝，删掉这一条，前面的字进半角盒。外侧其它缝不管。

```
文本。」文本
```

跨行的缝这一步不收。收完若下行字折上来、本行又有成对，再扫，直到本行没有可收的。

`applyComboSymbolsOnLine` · `combo.js`

## 5. 行尾处理（每行；段末行不做）

看下一行行头，分两岔。做完用上面的 selector。

### 5.1 下一行行头是 `不能在行头`

禁则打头：把下一行行头那些禁则抽回来，或把本行尾连禁则一起推下去。

**推出基数：** 从本行行尾往回，连续的 `不能在行头`（可以是 0 个），再加上再往前那一个可以出现在行头的字；再扣掉推出造成的连写。推完之后看**现在的本行行尾**：若是 `后有空`，收成半角盒；可悬挂 +1，不可悬挂 +0.5。

**压入基数：** 从下一行行头往下，连续的 `不能在行头`，碰到可以出现在行头的字就停；再扣掉压入造成的连写。若抽出的最后一个要收成半角，包半角；可悬挂 −1，不可悬挂 −0.5。

### 5.2 其余情况：撑行

下一行行头不是 `不能在行头`。用抽下一行来填本行；抽不动或抽太多就改推。

**压入基数：** 下一行行头连续的 `前有空`（可以是 0 个），再往后一个不是 `前有空` 的字，再往后连续的 `不能在行头`；再扣掉压入造成的连写。抽出的最后一个若是 `后有空`，包半角；可悬挂 −1，不可悬挂 −0.5。只抽那个字、把后面的 `」` 晾成新行头，等于没做禁则。

**推出基数：** 本行行尾连续的 `前有空`（可以是 0 个）；再扣掉推出造成的连写。推完看**现在的本行行尾**（没推走字就是现在这个尾）：若是 `后有空` 且还没包半角，收成半角盒；可悬挂 +1，不可悬挂 +0.5。已经是半角盒的，合里已经减过半，不要再加。

满行只差不到 0.4em 却要抽一个整字时，压入会超过 0.6em，selector 改推。可悬挂不占正文框，满行抽 `。` 基数是 0。满行抽 `」` 默认不可悬挂，基数仍 0.5。段末行不做这一步，短行保持短。行尾那个 `后有空` 右边的空也要收；推出的盒不占正文框，这条缝跟着进沟。占内口 0 之后正文框里多出来的空摊进本行行内缝撑满，盒才顶在行边进沟。压入量 ≤ 0 的「不抽缝」是不挤，不是不撑：抽完合仍短于行宽，剩余摊满**正文右缘**，不是摊进沟。第四步连写省下的 0.5em 也算正文剩余。

---

悬挂选项（无参不开；打开后默认右挂句读、缩进左挂、全局左挂关）：

```js
Katsuji.applyHangAvoidance(root, {
  hangingPunctuation: { hangLeftIndent: true, hangLeft: false, hangRight: 'stops' },
});
```

对应代码入口：`applyHangAvoidance` · `processLine` · `trySpaceOnEdgeStart` · `applyComboSymbolsOnLine` · `applyLineEndOnLine`。纯规则（基数、连写、selector、跳过）在 `typeset-rules.js`。
