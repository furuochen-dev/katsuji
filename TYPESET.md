# 排版过程

先 `Katsuji.apply`，再 `Katsuji.applyHangAvoidance`。切缝扫根下文本（跳过 `pre` / `code` 等）；挤推、撑行只动段落、标题、列表。Demo 的「下一步」是同一条线，每次只做成一件。

## 标点属性

默认表在 `punctuation-rules.js`。汉字、英文、数字不在表里：两边都不插缝，行头行尾都合法。

| 属性 | 字符（默认） |
|---|---|
| `前有空` | `（` `【` `「` `『` `《` `〈` `〔` `［` `“` `‘` `(` |
| `后有空` | `，` `。` `、` `；` `：` `？` `！` `）` `】` `｝` `〉` `》` `」` `』` `)` `]` `}` `”` `’` `·` `,` `.` `;` `:` `!` `?` |
| `两侧无空` | `…` `～` `—` `％` `‰` `℃` `°` |
| `不能在行头` | `后有空` 和 `两侧无空` 全部 |

`不能在行头` 不是另造一张表，就是 `后有空` + `两侧无空`。

`setPunctConfig` / `applyPunctPreset('jis-strict'|'vertical')` 会改这张表：JIS 严格把小假名、长音、叠字等并进 `两侧无空`；竖排把 `？` `！` 改成 `两侧无空`，并加上竖排形标点。

## 流水线

1. **标记可调整空间**  
   在标点外侧插一条宽度为 0 的缝，后面全靠改这条缝的空白来挤行、撑行。`后有空` 插在后面，`前有空` 插在前面，`两侧无空` 两边都不插。这段只该对同一段字跑一遍，再跑会再插一层缝。  
   `Katsuji.apply` · `segmenter.js`

2. **暂时禁用浏览器自动断行**  
   浏览器自己会把 `不能在行头` 拽回上一行，我们看不到行首有这类标点，后面的修正就没机会做。先改成任意字都可以折行，排完也不改回来。  
   `relaxBuiltinLineBreak` · `line-break.js`

3. **标点连写间距收缩**  
   `后有空` 后面紧跟任意标点，或任意标点后面紧跟 `前有空` 时，把当中那条缝收掉半个字，并标成「连写、后面别再动」。连续的 `两侧无空`（如 `……`）绑在一起，中间不折开。  
   `applyComboSymbolsBlock` · `glueAdjacentNonePunct` · `combo.js`

4. **撤掉上次的行调整，缝留着**  
   调整可以反复跑。缝上一次挤过、撑过，标点上可能还套着半角盒子；不先拿掉，新的一轮会叠在旧结果上。连写收过的缝不动。  
   `resetGapStyles` · `unwrapHalfPunctInBlock`

5. **行边界的空**  
   行头行尾碰到该收半空的标点：收掉不该占行的那半个字。先做完这一类，再做非法符号。一次只做成一件。  
   `applySpaceOnEdge` · `space-on-edge.js`

    1. **行首开括号顶格**  
       行首是 `前有空`：去掉它左边的前空。左边那条缝清零并标成不再动；包一个 0.5em 宽的窗口，里面的字左移半个字，只露出右边的墨，左边的空不占行。不是首行时和后面第一个字绑在一起不拆开，半宽后仍超过上一行剩余，不会被抽回去。  
       `trySpaceOnEdgeStart`

    2. **行尾标点占宽**  
       行尾是 `后有空`：挤或推缝。若选择推，先把行尾连续 `后有空` 里的闭括（如 `」`）收成半角盒再推缝。挤进会把下一行行首抽到本行行尾；下一行开头是 `前有空`（行尾非法）时不能挤，直接推。量行宽时，字按 DOM 逐字加；组合符号的 −0.5em 只算两侧都在本行的；`」` 在行尾、`「` 已折到下一行的那条 combo 不缩短本行。推出只改本行行内的缝，不把下一行 `「` 前的开括号空撑开。末行不做。  
       `trySpaceOnEdgeEnd` · `hangMarginEmPerGap` · `line-width.js`

6. **行边界的非法符号**  
   行首是 `不能在行头`、或行尾是 `前有空`：改缝把字挤回去或推出去。一次只做成一件。  
   `applyIllegalOnEdge` · `illegal-on-edge.js`

    1. **行首非法字符**  
       行首是 `不能在行头`：改上一行的缝。连续多个按合计宽度挤。缝上已经挤过就不再挤。若是 `后有空` 且选择挤进，把行首标点收成半个字宽的盒子（字仍画全，多出的半个字挂在盒外）。若选择推出，先把上一行行尾连续 `后有空` 里的闭括（如 `」`）收成半角盒再推缝，避免只推整字宽、行尾 `」` 带着前一个字一起掉下去。  
       `tryIllegalOnEdgeStart`

    2.    **行尾非法开括号**  
       行尾是 `前有空`：只改本行的缝，不包半角盒子。连写缝、这个开括号旁边的缝不动；能动的缝少于两条就跳过。  
       `tryIllegalOnEdgeEnd`

   第 5、6 步在同一段循环里轮流试，先空后非法；一段最多 24 轮。挤还是推看哪边动得少，差不多时默认挤。挤进量须为正且不超过 0.5em（允许多 0.01em），否则有推出就推、没有就跳过。要挤或推的量不是正数、或每条缝分到不到 0.01em，就当没这回事。  
   `applyProcessPunct`

7. **没齐的行撑满**  
   缺多少宽，均摊到这一行还能动的缝上。末行不撑（源码缩进留下的空行不计入行数）。连写缝、行首已顶格的 `前有空` 缝、行最末那条缝都不动。行尾若是 `后有空`，先收成半角盒再量余量；行尾连续 `后有空` 里优先收闭括（如 `」`）。余量按「合」（字+空−半）来，和行宽分解同一套数。  
   `applyLineSurplusPaddingByVisualWidth` · `surplus.js`
