# 排版过程

先 `Katsuji.apply`，再 `Katsuji.applyHangAvoidance`。切缝扫根下文本（跳过 `pre` / `code` 等）；挤推、撑行只动段落、标题、列表。

Demo 的「下一步」一次做完**一行**（第 3–5 步）。段首行跳过第 3 步，段末行跳过第 5 步。

## 标点属性

默认表在 `punctuation-rules.js`。汉字、英文、数字不在表里：两边都不插缝，行头行尾都合法。

| 属性 | 字符（默认） |
|---|---|
| `前有空` | `（` `【` `「` `『` `《` `〈` `〔` `［` `“` `‘` `(` |
| `后有空` | `，` `。` `、` `；` `：` `？` `！` `）` `】` `｝` `〉` `》` `」` `』` `)` `]` `}` `”` `’` `·` `,` `.` `;` `:` `!` `?` |
| `两侧无空` | `…` `～` `—` `％` `‰` `℃` `°` |
| `不能在行头` | `后有空` 和 `两侧无空` 全部 |

`不能在行头` 不是另造一张表，就是 `后有空` + `两侧无空`。

行尾禁则没有单独一张表：**不能出现在行尾的就是 `前有空`**。

`setPunctConfig` / `applyPunctPreset('jis-strict'|'vertical')` 会改这张表：JIS 严格把小假名、长音、叠字等并进 `两侧无空`；竖排把 `？` `！` 改成 `两侧无空`，并加上竖排形标点。

## 怎么跑

1、2 对整篇文章做一次。之后每一段从第一行做到倒数第二行：一行上先 3、再 4、再 5，做完再下一行。第 5 步要看**还没处理的下一行**，所以从前往后。

| 谁 | 不做 |
|---|---|
| 段首行 | 第 3 步 |
| 段末行 | 第 5 步 |

`applyHangAvoidance` 开头先清掉上一轮挤推和半角盒，连写锁住的缝留下。排完不恢复浏览器断行，`line-break: anywhere` 留着。

第 3、4 步**只看本行里的字**。跨行的 `」` / `「` 不当连写。

## 量宽和 selector

行宽分解与面板相同：**合 = 字 + 空 − 半**。字按 DOM 逐字加；空是本行缝的 padding/margin；半是半角盒修正。跨行连写不缩短本行。

第 5 步的基数是这些字**挪完之后**的占宽，不是毛宽相加。再和行宽合成：

- 压入量 = 基数 + 合 − 行宽
- 推出量 = 基数 + 行宽 − 合

未包半角的 `后有空` 先按 1em 计；决定包半角时，基数再按各步的 +0.5 / −0.5 改。汉字、`前有空`、`两侧无空` 未顶格时按 1em。

挪完会出现新的成对标点（第 4 步那种）。每锁死一处 −0.5em，都要从基数里减：

- **压入：** 原行尾 + 抽上来的第一个字；以及抽上来的那一串内部（下一行还没做第 4 步，串内尚未锁）。抽完立刻按第 4 步锁这些缝。
- **推出：** 推下去的最后一个字 + 原下一行行头。串内若本行第 4 步已经锁过，已在「合」里，不要再减一遍。

不减的话，差 0.5em 去抽 `。」` 会按 1em 计，selector 可能改推；实际连写后只多占 0.5em，本该压入。推到下一行头碰上 `前有空` 同理。

然后 selector：

- 压入不超过 0.5em（允许多 0.01em）。超上限：有推出就推、没有就跳过
- 压入量 ≤ 0（剩余已够放下）：仍压入，不抽缝，没有可调缝也可以
- 压入量 > 0：要有可调缝来摊，挤还是推看哪边每条缝摊到的绝对值更小，差不多时默认挤
- 推出量不是正数、或每条缝分到不到 0.01em，当没这回事

哪几条缝能摊：

- **压入：** 本行行内缝，加上行尾 `后有空` 后面那条。抽上来之后这条变成接缝，要拿来摊压入量。本行没有别的缝、只剩这一条，也可以抽。
- **推出：** 只改本行行内缝。行最末那条缝、连写缝、行首已顶格锁死的缝都不动，也不把下一行 `前有空` 前面那条开括号缝撑开。

---

## 1. 标记可调整空间（整篇）

在标点外侧插一条宽度为 0 的缝，后面全靠改这条缝的空白来挤、推。`后有空` 插在后面，`前有空` 插在前面，`两侧无空` 两边都不插。同一段字只该跑一遍，再跑会再插一层缝。

`Katsuji.apply` · `segmenter.js`

## 2. 暂时禁用浏览器自动断行（整容器）

浏览器自己会把 `不能在行头` 拽回上一行，后面的修正就看不到行首禁则。先改成任意字都可以折行，排完也不改回来。

`relaxBuiltinLineBreak` · `line-break.js`

## 3. 行首开括号顶格（每行；段首行不做）

行首是 `前有空`：去掉它左边的前空。左边那条缝清零并标成不再动；包一个 0.5em 宽的窗口，里面的字左移半个字，只露出右边的墨。不是段首行时和后面第一个字绑在一起不拆开，半宽后仍超过上一行剩余，不会被抽回去。

```
（文本文本文本...
```

`trySpaceOnEdgeStart`

## 4. 标点连写间距收缩（每行，只看行内）

成对条件：`后有空` 后面紧跟任意标点，或任意标点后面紧跟 `前有空`。连续 `两侧无空`（如 `……`）绑在一起，中间不折开。

**4.1** `后有空` 紧跟 `前有空`（如 `）（`）：中间有两条缝，只把**前一条**（后有空后面那条）收成 −0.5em 并锁死，后一条不动。

```
文本）（文本文本...
```

**4.2** 其余成对：两字中间只有一条缝，把这一条收成 −0.5em 并锁死。外侧其它缝不管。

```
文本。」文本
```

跨行的缝这一步不锁。

`applyComboSymbolsOnLine` · `combo.js`

## 5. 行尾处理（每行；段末行不做）

看下一行行头，分两岔。做完用上面的 selector。

### 5.1 下一行行头是 `不能在行头`

禁则打头：把下一行行头那些禁则抽回来，或把本行尾连禁则一起推下去。

**推出基数：** 从本行行尾往回，连续的 `不能在行头`（可以是 0 个），再加上再往前那一个可以出现在行头的字；再扣掉推出造成的连写。推完之后看**现在的本行行尾**：若是 `后有空`，基数 +0.5，并把这个新行尾收成半角盒。

**压入基数：** 从下一行行头往下，连续的 `不能在行头`，碰到可以出现在行头的字就停；再扣掉压入造成的连写。若抽出的最后一个要收成半角，基数 −0.5 并包。

### 5.2 其余情况：撑行

下一行行头不是 `不能在行头`。用抽下一行来填本行；抽不动或抽太多就改推。

**压入基数：** 下一行行头连续的 `前有空`（可以是 0 个），再往后一个不是 `前有空` 的字；再扣掉压入造成的连写。那一个若是 `后有空`，基数 −0.5 并包半角。

**推出基数：** 本行行尾连续的 `前有空`（可以是 0 个）；再扣掉推出造成的连写。推完看**现在的本行行尾**（没推走字就是现在这个尾）：若是 `后有空` 且还没包半角，基数 +0.5，并收成半角盒。已经是半角盒的，合里已经减过半，不要再加。

满行只差 0.5em 却要抽一个整字时，压入会超过 0.5em，selector 改推。段末行不做这一步，短行保持短。行尾那个 `后有空` 右边的空也要收，墨才能顶到行边；只往缝里填、不收这个空，行尾会留半个字的白。

---

对应代码入口：`applyHangAvoidance` · `processLine` · `trySpaceOnEdgeStart` · `applyComboSymbolsOnLine` · `applyLineEndOnLine`。纯规则（基数、连写、selector、跳过）在 `typeset-rules.js`。
